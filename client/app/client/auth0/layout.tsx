import React from 'react'

type Props = { children: React.ReactNode }

export default function PageLayout({ children }: Props) {
  // todo implement auth0 layout and auth0 Provider
  return <>{children}</>
}
